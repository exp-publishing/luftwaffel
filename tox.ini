[tox]
testenv = sandbox

[testenv]
basepython = python3

install_command = pip install {opts} {packages}
deps = -rrequirements.txt
    pytest
    pytest-asyncio
    pudb

[testenv:sandbox]
commands = py.test {posargs}
